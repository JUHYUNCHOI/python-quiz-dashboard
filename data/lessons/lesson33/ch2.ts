import { Chapter } from '../types'

export const ch2: Chapter = {
  id: "ch2",
  title: "기본값 (Default Value)",
  emoji: "⚙️",
  steps: [
    {
      id: "ch2-1",
      type: "explain",
      title: "💭 매번 같은 값을 넣어야 한다면?",
      content: `💭 친구 5명에게 인사하는 함수가 있어. 근데 매번 **'안녕'**을 입력해야 해... 뭔가 줄일 수 있는 방법 없을까?

\`\`\`python
def 인사(이름, 메시지):
    print(f'{메시지}, {이름}!')

인사('철수', '안녕')
인사('영희', '안녕')
인사('민수', '안녕')
인사('지수', '안녕')
인사('혜진', '안녕')
\`\`\`

**'안녕'을 5번이나 썼어요!** 😩

대부분 "안녕"이라고 하는데, 가끔만 다른 인사를 써요. 그런데 *거의 안 바뀌는 값* 을 매번 손으로 적어주는 게 너무 귀찮죠.

생각해보면 우리 일상에도 이런 게 많아요. 카페에서 "아이스 아메리카노" 를 시키면 보통 **기본 사이즈** 가 나와요. 매번 "보통 크기로 주세요" 라고 말 안 해도 되죠. 특별히 큰 걸 원할 때만 "라지로요!" 라고 하면 돼요.

함수도 똑같이 만들 수 있으면 좋겠어요. 안 적으면 **알아서 '안녕'** 이 들어가고, 특별히 다른 인사를 원할 때만 그 값을 주는 거예요.

@핵심: 자주 쓰는 값을 **매번 안 써도** 자동으로 넣어주는 방법이 필요하다!`
    },
    {
      id: "ch2-2",
      type: "explain",
      title: "💡 기본값으로 해결!",
      content: `💭 매개변수에 **미리 값을 넣어놓으면** 안 줄 때 자동으로 쓰이면 좋겠는데... 어떻게 쓸까?

\`\`\`python
def 인사(이름, 메시지='안녕'):  # 👈 기본값!
    print(f'{메시지}, {이름}!')
\`\`\`

**=** 뒤에 기본값을 넣으면 끝! 이건 *"메시지를 안 주면 이걸 써"* 라고 미리 약속해두는 거예요.

\`\`\`python
인사('철수')             # 메시지 안 줌 → "안녕, 철수!"
인사('영희', '반가워')   # 메시지 줌 → "반가워, 영희!"
\`\`\`

- 값을 **안 주면** → 기본값 사용! ✅
- 값을 **주면** → 준 값 사용! ✅ (준 값이 기본값을 *덮어써요*)

> 💡 기본값은 **"선택 입력칸"** 을 만드는 거예요. 이름은 *꼭* 줘야 하지만, 메시지는 *원하면* 주는 옵션이 되는 거죠. 그래서 가장 흔한 경우(\`인사('철수')\`)는 짧게 쓰고, 특별한 경우만 값을 더 붙이면 돼요.

@핵심: 매개변수에 **=값**을 써두면, 안 줄 때 자동으로 그 값이 사용된다!`
    },
    {
      id: "ch2-3",
      type: "interactive",
      title: "🎬 기본값 동작 체험!",
      description: "클릭하며 기본값이 어떻게 동작하는지 직접 확인해보세요!",
      component: "defaultValueVisualizer"
    },
    {
      id: "ch2-4",
      type: "interactive",
      title: "따라 써보기: 기본값",
      description: "기본값이 있는 함수를 따라 써보세요!",
      component: "typeAlong",
      targetCode: `def 인사(이름, 메시지='안녕'):
    print(f'{메시지}, {이름}!')

인사('철수')
인사('영희', '반가워')`,
      expectedOutput: "안녕, 철수!\n반가워, 영희!"
    },
    {
      id: "ch2-5",
      type: "quiz",
      title: "🛑 멈추고 예측!",
      content: `다음 코드의 출력 결과는?

\`\`\`python
def 거듭제곱(n, 지수=2):
    return n ** 지수

print(거듭제곱(3))
\`\`\``,
      options: ["3", "6", "9", "에러 발생"],
      answer: 2,
      explanation: "지수를 안 줬으니 기본값 2가 사용돼요! 3² = 3 × 3 = 9"
    },
    {
      id: "ch2-6",
      type: "interactive",
      title: "빈칸 채우기: 기본값",
      description: "기본값을 설정해보세요!",
      component: "fillInBlank",
      codeTemplate: "def 인사(이름, 메시지___1___):\n    print(f'{메시지}, {이름}!')\n\n인사('철수')",
      blanks: [{ id: "1", answer: "='안녕'", hint: "기본값은 = 뒤에 써요!" }],
      choices: ["='안녕'", ":'안녕'", "=='안녕'", "->안녕"],
      expectedOutput: "안녕, 철수!"
    },
    {
      id: "ch2-7",
      type: "explain",
      title: "🚨 기본값 순서 규칙!",
      content: `💭 기본값이 있는 매개변수랑 없는 매개변수를 **섞어서** 쓸 때, 순서를 아무렇게나 해도 될까?

\`\`\`python
# ❌ 에러!
def 함수(a=1, b):
    return a + b

# ✅ OK!
def 함수(a, b=1):
    return a + b
\`\`\`

**규칙:**
- 기본값 **없는** 매개변수 → **앞**에
- 기본값 **있는** 매개변수 → **뒤**에

**왜 이런 규칙이 있을까?** 값을 그냥 순서대로 넣으면(위치 인자), 파이썬은 **앞에서부터 차례로** 채워요. 그런데 \`def 함수(a=1, b)\` 처럼 기본값 있는 게 앞에 오면 문제가 생겨요.

\`\`\`python
def 함수(a=1, b):   # ❌
    return a + b

함수(5)   # 이 5는 a 일까, b 일까? 🤔
\`\`\`

\`5\` 하나만 줬을 때 — 앞에 있는 \`a\` 에 넣자니 \`a\` 는 이미 기본값 1이 있고, 그럼 \`b\` 는 값을 못 받아요. 파이썬 입장에선 *"생략 가능한 칸이 먼저, 꼭 채워야 하는 칸이 뒤"* 라서 어디부터 채워야 할지 규칙이 꼬여버려요. 그래서 파이썬이 아예 **함수를 만드는 순간 에러** 를 내요 — 헷갈리는 상황을 미리 막는 거죠.

> ⚠️ 외우는 팁: **"꼭 줘야 하는 것 먼저, 안 줘도 되는 것 나중에."** \`def 주문(메뉴, 수량=1)\` 처럼 — 메뉴는 필수, 수량은 선택!

@핵심: 기본값 있는 매개변수는 **반드시 뒤쪽**에 와야 한다!`
    },
    {
      id: "ch2-8",
      type: "quiz",
      title: "순서 퀴즈!",
      content: "다음 중 올바른 함수 정의는?",
      options: ["def f(a=1, b): ...", "def f(a, b=1): ...", "def f(=1, b): ...", "def f(a, =1): ..."],
      answer: 1,
      explanation: "기본값이 있는 b=1이 뒤에 와야 해요!"
    },
    {
      id: "ch2-9",
      type: "mission",
      title: "💰 할인 함수 만들기",
      task: "기본 할인율 10%를 적용하는 함수를 완성하세요!",
      initialCode: `def discount(price, rate=10):
    # 할인율 rate% 를 뺀 최종 가격을 계산해서
    # 여기에 return 문으로 돌려주세요!


print(discount(10000))       # 10% 할인 → 9000
print(discount(10000, 20))   # 20% 할인 → 8000`,
      expectedOutput: "9000.0\n8000.0",
      hint: "할인된 가격 = price * (100 - rate) / 100",
      hint2: "return price * (100 - rate) / 100"
    }
  ]
}
