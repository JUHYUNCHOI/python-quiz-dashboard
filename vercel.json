{
  "git": {
    "deploymentEnabled": {
      "main": true,
      "dev": false
    }
  },
  "ignoreCommand": "if [ -n \"$VERCEL_GIT_COMMIT_REF\" ] && [ \"$VERCEL_GIT_COMMIT_REF\" != \"main\" ]; then exit 0; fi; git diff --quiet HEAD^ HEAD -- . ':!.claude' ':!*.md' ':!scripts/*.py'"
}
